import { OverlayWishlistDirective } from './overlay-wishlist.directive';

describe('OverlayWishlistDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayWishlistDirective();
    expect(directive).toBeTruthy();
  });
});
